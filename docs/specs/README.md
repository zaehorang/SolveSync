# 동작 명세

> **Description**: 각 Coding Platform 연동을 그 기술을 모르는 사람이 검수할 수 있도록 관측 가능한 동작으로만 기술한 명세 모음이다. 구현 계약은 [`docs/platforms/`](../platforms/README.md)를 따른다.

이 폴더의 문서는 [`docs/platforms/`](../platforms/README.md)를 **대체하지 않는다.** 두 문서는 독자가 다르다.

| | `docs/specs/` (이 폴더) | `docs/platforms/` |
|---|---|---|
| 독자 | 그 기술을 모르는 사람 | Adapter를 고칠 사람 |
| 답하는 질문 | 사용자에게 무엇이 보이는가, 이게 맞는 동작인가 | 어느 DOM 사실 위에 무엇을 구현했는가 |
| 담는 것 | 발동 계기, 입력, 결과, 경계 상황, 손으로 확인 가능한 검증 항목 | selector, 관찰 날짜, mutation 순서, 식별자 형식 |
| 담지 않는 것 | 클래스·메서드 이름, 코드 블록, 프레임워크 용어 | 사용자 관점 재서술 |

둘이 어긋나면 **구현이 무엇을 하는지는 `docs/platforms/`가 맞고, 그것이 옳은 동작인지는 이 폴더의 열린 질문으로 올린다.**

## 명세는 플랫폼마다 다르게 갈린다

세 플랫폼을 같은 다섯 조각으로 자르지 않았다. 경계는 "이 동작만 사라지면 사용자가 무엇을 못 하게 되는가"로 정하는데, 그 답이 플랫폼마다 다르기 때문이다. 개수가 다른 것 자체가 세 연동이 실제로 얼마나 다른지를 보여준다.

| | 명세 수 | 다른 둘에 없는 것 | 이유 |
|---|---|---|---|
| [LeetCode](#leetcode) | 6 | 풀이 조회, 조회 실패, 저장 실패 다시 시도 | 코드를 화면이 아니라 플랫폼 서버에서 가져온다. 그래서 로그인이 필요하고 실패가 여러 갈래다 |
| [Programmers](#programmers) | 5 | — | 화면에서 코드를 그 자리에서 읽는다. 가장 단순한 형태다 |
| [SWEA](#swea) | 6 | 문제 식별, 코드 가져오기 | 모든 문제가 같은 주소를 써서 문제 식별이 독립 동작이고, 코드를 읽는 데 기다림이 있다 |

## LeetCode

| 명세 | 사라지면 사용자가 잃는 것 |
|---|---|
| [정답 감지](leetcode/accepted-detection.md) | 정답을 받아도 아무것도 시작되지 않는다 |
| [풀이 조회](leetcode/submission-lookup.md) | 무엇을 올릴지 정할 수 없다 |
| [풀이를 조회하지 못했을 때](leetcode/lookup-failure.md) | 조회가 실패해도 사용자가 모른다 |
| [같은 제출 중복 방지](leetcode/duplicate-prevention.md) | 같은 제출이 두 번 커밋된다 |
| [저장 실패 다시 시도](leetcode/retry.md) | 실패한 풀이를 올리려고 문제를 다시 제출해야 한다 |
| [저장 위치와 목록](leetcode/repository-layout.md) | 어디에 어떤 모양으로 쌓이는지 확인할 수 없다 |

## Programmers

| 명세 | 사라지면 사용자가 잃는 것 |
|---|---|
| [정답 감지](programmers/accepted-detection.md) | 정답을 받아도 저장소에 아무것도 올라가지 않는다 |
| [풀이 확정](programmers/solution-snapshot.md) | 정답을 낸 코드가 아니라 그 뒤에 고친 코드가 올라간다 |
| [같은 풀이 중복 방지](programmers/duplicate-prevention.md) | 같은 풀이를 제출할 때마다 커밋이 쌓인다 |
| [풀이를 읽지 못했을 때](programmers/extract-failure.md) | 실패했는데 실패한 줄 모른다 |
| [저장 위치와 목록](programmers/repository-layout.md) | 어디에 어떤 모양으로 쌓이는지 확인할 수 없다 |

## SWEA

| 명세 | 사라지면 사용자가 잃는 것 |
|---|---|
| [정답 감지](swea/accepted-detection.md) | 정답을 받아도 아무것도 시작되지 않는다 |
| [문제 식별](swea/problem-identity.md) | 모든 문제가 한 문제로 뭉개지거나 저장 자체가 되지 않는다 |
| [코드 가져오기](swea/code-retrieval.md) | 코드가 잘리거나 아예 오지 않는다 |
| [풀이를 읽지 못했을 때](swea/extract-failure.md) | 실패가 조용히 지나간다 |
| [같은 풀이 중복 방지](swea/duplicate-prevention.md) | 같은 풀이가 반복해 쌓인다 |
| [저장 위치와 목록](swea/repository-layout.md) | 어디에 어떤 모양으로 쌓이는지 확인할 수 없다 |

## 검수하는 법

각 명세의 `Acceptance` 항목은 **화면만 보고 손으로 따라 할 수 있게** 쓴다. 코드를 열어야 확인할 수 있으면 그것은 검증 항목이 아니라 결과 서술이다.

`열린 질문`은 코드만으로 답이 나오지 않아 사람에게 물어야 하는 것이다. 답이 정해지면 그 질문을 지우고, 결정이 동작을 바꾸면 해당 source of truth를 먼저 고친 뒤 명세를 갱신한다.

같은 이름의 명세를 세 플랫폼에서 나란히 읽으면 연동 사이의 차이가 드러난다. 특히 중복 방지는 결론이 갈린다 — LeetCode는 제출마다 번호가 달라 같은 코드를 다시 내면 커밋이 하나 더 생기고, 나머지 둘은 코드 내용이 판정에 들어가 커밋이 생기지 않는다.

## 새 명세를 쓸 때

파일 단위가 아니라 "사용자 눈에 하나로 보이는 동작" 단위로 자른다. 경계가 애매하면 "이 동작만 사라지면 사용자가 무엇을 못 하게 되는가"로 물어 답이 한 문장이면 하나의 기능이다.

플랫폼 폴더 아래에 두고 파일 이름에는 플랫폼 이름을 넣지 않는다. 폴더가 이미 말하는 것을 파일 이름이 반복하면 목록이 접두사로 뒤덮인다.

작성 규칙과 자동 검사는 `behavior-spec-extraction` 스킬이 들고 있다. 구현 용어가 새어 들어갔는지, 검증할 수 없는 항목이 있는지를 그 스킬의 린터가 잡는다.
